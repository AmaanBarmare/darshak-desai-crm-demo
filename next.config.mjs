/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the dev-only overlay badge; it overlaps the sidebar footer in dev.
  devIndicators: false,
};

export default nextConfig;
